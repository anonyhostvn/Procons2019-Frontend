


export const actions = (state) => state.AppReducers.formInformation;
export const teamId  = (state) => state.AppReducers.id;
export const mapId = (state) => state.AppReducers.mapId;
export const token = (state) => state.AppReducers.token;